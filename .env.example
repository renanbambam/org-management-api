# Copy to .env and fill in real values. Never commit .env.

# MongoDB
DB_URI=mongodb://localhost:27017/manager

# Access token
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRE=15m

# Refresh token
RT_SECRET=replace-with-a-different-long-random-secret
RT_EXPIRE=7d

# Server
PORT=3000
